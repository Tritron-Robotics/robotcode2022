# robotcode2022 Github Pages
## What is this branch?
This branch is a separate branch that has all the content to host the site for this project. This branch should never be merged with the main branch.

## Javadoc
This branch is overwritten every time that the deploy-javadoc action runs(when the master branch is updated).
